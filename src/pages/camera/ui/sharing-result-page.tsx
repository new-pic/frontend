// 실시간 공유 종료 후 저장할 사진 선택 화면

import { VStack } from "@shared/ui";
import { SafeAreaView } from "react-native-safe-area-context";

export function SharingResultPage() {
  return (
    <SafeAreaView>
      <VStack className="h-full px-8 py-4"></VStack>
    </SafeAreaView>
  );
}
