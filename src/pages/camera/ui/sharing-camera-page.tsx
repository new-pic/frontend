// 실시간 카메라 공유 화면

import { VStack } from "@shared/ui";
import { SafeAreaView } from "react-native-safe-area-context";

export function SharingCameraPage() {
  return (
    <SafeAreaView>
      <VStack className="h-full px-8 py-4"></VStack>
    </SafeAreaView>
  );
}
