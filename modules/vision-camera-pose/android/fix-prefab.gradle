afterEvaluate {
  tasks.matching { it.name.startsWith("configureCMake") }.configureEach {
    dependsOn(":react-native-nitro-modules:prefabReleasePackage")
    dependsOn(":react-native-vision-camera:prefabReleasePackage")
  }
}
